<UserControl
    x:Class="FreelanceApp.Windows.AdminControls.RolesManagementControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:FreelanceApp.Windows.ViewModels"
    mc:Ignorable="d"
    d:DesignWidth="800"
    d:DesignHeight="450"
    d:DataContext="{d:DesignInstance Type=vm:UserManagementViewModel}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="Bool2Vis"/>
        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель -->
        <StackPanel Grid.Column="0" Margin="0,0,10,0">

            <!-- Кнопки действий -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Margin="5" Content="Добавить"    Command="{Binding AddCommand}"/>
                <Button Margin="5" Content="Редактировать" Command="{Binding EditCommand}"/>
                <Button Margin="5" Content="Удалить"      Command="{Binding DeleteCommand}"/>
            </StackPanel>

            <!-- Форма -->
            <Border Padding="10" Background="#F9F9F9"
                    Visibility="{Binding IsFormOpen, Converter={StaticResource Bool2Vis}}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>

                        <!-- ID (только при удалении) -->
                        <StackPanel Visibility="{Binding IsDeleteMode, Converter={StaticResource Bool2Vis}}">
                            <TextBlock Text="ID роли"/>
                            <TextBox Text="{Binding FormId}" IsReadOnly="True"/>
                        </StackPanel>

                        <TextBlock Margin="0,5,0,0" Text="Название роли"/>
                        <TextBox Text="{Binding RoleName, UpdateSourceTrigger=PropertyChanged}"
                                 IsReadOnly="{Binding IsRoleNameReadOnly}"/>

                        <TextBlock Margin="0,5,0,0" Text="Привилегии (JSON)"/>
                        <TextBox Height="120" AcceptsReturn="True" TextWrapping="Wrap"
                                 FontFamily="Consolas"
                                 Text="{Binding PrivilegesJson, UpdateSourceTrigger=PropertyChanged}"/>

                        <StackPanel Margin="0,10,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Width="90" Margin="5" Content="Сохранить" Command="{Binding SaveCommand}"/>
                            <Button Width="90" Margin="5" Content="Отмена"   Command="{Binding CancelCommand}"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </StackPanel>

        <!-- Таблица ролей -->
        <DataGrid Grid.Column="1"
                  ItemsSource="{Binding Roles}"
                  SelectedItem="{Binding SelectedRole}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Role ID" Binding="{Binding Id}" SortMemberPath="Id">
                    <DataGridTextColumn.SortDirection>Ascending</DataGridTextColumn.SortDirection>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Role Name"       Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Role Privileges" Binding="{Binding Privileges}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
